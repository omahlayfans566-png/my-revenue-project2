# Prevent npm from skipping optional dependencies on Linux (Render/CI).
# Rollup requires platform-specific optional binaries (@rollup/rollup-linux-x64-gnu).
# Without this, builds fail with "Cannot find module @rollup/rollup-linux-x64-gnu".
optional=true
